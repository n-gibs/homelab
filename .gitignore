autoinstall/.secrets
autoinstall/user-data

# Ansible
*.retry
.vault_pass

# Local secrets (never commit)
.secrets
.secrets.generated
pub-cert.pem

# Built configs with injected secrets (never commit)
config/
