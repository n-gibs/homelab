enableRbac: true
serviceAccount:
  create: true

env:
  HOMEPAGE_ALLOWED_HOSTS: "home.nik-homelab.dev"

config:
  settings.yaml: |
    title: Homelab
    theme: dark
    color: slate
    headerStyle: boxed
    target: _blank
    allowedHosts:
      - home.nik-homelab.dev

  kubernetes.yaml: |
    mode: cluster

  widgets.yaml: |
    - resources:
        cpu: true
        memory: true
    - search:
        provider: duckduckgo
        target: _blank
    - greeting:
        text_size: xl
        text: Homelab

  bookmarks.yaml: |
    []
