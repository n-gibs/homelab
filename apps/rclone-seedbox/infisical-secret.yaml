apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalStaticSecret
metadata:
  name: rclone-seedbox-sftp
  namespace: rclone-seedbox
spec:
  infisicalAuthRef:
    name: infisical-auth
    namespace: infisical
  syncOptions:
    refreshInterval: 60s
  sources:
    - projectSlug: homelab-ef-28
      environmentSlug: prod
      secretPath: /rclone-seedbox/rclone-seedbox-sftp
  targets:
    - name: rclone-seedbox-sftp
      namespace: rclone-seedbox
      kind: Secret
      creationPolicy: Owner
