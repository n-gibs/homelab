---
- name: Apply common baseline to all nodes
  hosts: k3s_cluster
  roles:
    - common

- name: Deploy k3s cluster
  ansible.builtin.import_playbook: k3s.orchestration.site

- name: Configure NFS server on worker-01
  hosts: worker-01
  roles:
    - nfs_server

- name: Configure cert-manager ClusterIssuers on control-plane
  hosts: worker-00
  roles:
    - cert_manager_issuers

# Uncomment when G6 is provisioned
# - name: Configure NFS client on control-plane-02
#   hosts: control-plane-02
#   roles:
#     - nfs_client
