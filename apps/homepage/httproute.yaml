apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
spec:
  parentRefs:
    - name: homelab
      namespace: envoy-gateway-system
  hostnames:
    - home.nik-homelab.dev
  rules:
    - backendRefs:
        - name: homepage
          port: 3000
