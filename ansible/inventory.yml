---
k3s_cluster:
  children:
    server:
      hosts:
        control-plane-01:
          ansible_host: 192.168.1.TODO  # TODO: fill in control plane (G4) IP
    agent:
      hosts:
        worker-01:
          ansible_host: 192.168.1.28   # G9: i5 12th gen, 16GB RAM, 12TB USB drive
        worker-02:
          ansible_host: 192.168.1.TODO  # TODO: fill in worker 2 (G6) IP

  vars:
    ansible_user: homelab
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
