apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-db-backup
  namespace: nextcloud
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  resources:
    requests:
      storage: 5Gi
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: nextcloud-db-backup
  namespace: nextcloud
spec:
  schedule: "45 3 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            # Alloy derives the Loki `app` stream label from this; without it these pods
            # fall back to the namespace and land in nextcloud's main log stream.
            app.kubernetes.io/name: nextcloud-db-backup
        spec:
          restartPolicy: OnFailure
          containers:
            - name: pg-dump
              image: ghcr.io/cloudnative-pg/postgresql:18-standard-trixie
              env:
                - name: PGHOST
                  value: nextcloud-database-rw
                - name: PGUSER
                  valueFrom:
                    secretKeyRef:
                      name: nextcloud-database-app
                      key: username
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: nextcloud-database-app
                      key: password
              command:
                - /bin/bash
                - -c
                - |
                  set -euo pipefail
                  out="/backup/nextcloud-$(date +%Y%m%d-%H%M%S).dump"
                  pg_dump -d app -Fc -f "$out"
                  echo "wrote $out ($(stat -c %s "$out") bytes)"
                  find /backup -name 'nextcloud-*.dump' -mtime +7 -delete
                  echo "retained: $(ls -1 /backup/nextcloud-*.dump | wc -l) dumps"
              volumeMounts:
                - name: backup
                  mountPath: /backup
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
          volumes:
            - name: backup
              persistentVolumeClaim:
                claimName: nextcloud-db-backup
