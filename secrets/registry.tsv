# Registry of static secrets sealed from secrets/.secrets via `just seal`.
# Add a row to add a secret. Values live in secrets/.secrets (gitignored), never here.
# columns: secret-name  namespace  outfile  key=ENVVAR[,key=ENVVAR...]
# ENVVAR prefixed `generate:` is auto-generated once and cached in secrets/.secrets.generated
# (gitignored) instead of requiring a value in secrets/.secrets — use for arbitrary internal
# shared secrets, never for credentials that must match an external system.
infisical-secrets        infisical      system/infisical/infisical-secrets.yaml           ENCRYPTION_KEY=generate:INFISICAL_ENCRYPTION_KEY,AUTH_SECRET=generate:INFISICAL_AUTH_SECRET,SITE_URL=INFISICAL_SITE_URL
infisical-machine-identity infisical    system/infisical/machine-identity.yaml           clientId=INFISICAL_CLIENT_ID,clientSecret=INFISICAL_CLIENT_SECRET
