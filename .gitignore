# Ansible
*.retry
.vault_pass
ansible/vault.yml

# Local secrets (never commit)
.secrets
.secrets.generated
pub-cert.pem

# Built configs with injected secrets (never commit)
config/
node-compile-cache/
