# networkPolicies.restrictInternalTraffic defaults on from 1.12.1, and Cilium enforces it. If
# instance-manager gRPC starts failing after a bump, that value is the first thing to set false.
chartName: longhorn
chartRepo: https://charts.longhorn.io
chartVersion: 1.12.1
syncWave: "1"
