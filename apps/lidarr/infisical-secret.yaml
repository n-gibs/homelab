apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalStaticSecret
metadata:
  name: arr-api-key
  namespace: lidarr
spec:
  infisicalAuthRef:
    name: infisical-auth
    namespace: infisical
  syncOptions:
    refreshInterval: 60s
  sources:
    - projectSlug: homelab-ef-28
      environmentSlug: prod
      secretPath: /lidarr/arr-api-key
  targets:
    - name: arr-api-key
      namespace: lidarr
      kind: Secret
      creationPolicy: Owner
