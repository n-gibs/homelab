controllers:
  main:
    pod:
      nodeSelector:
        homelab.io/storage: "true"
    containers:
      app:
        image:
          repository: lscr.io/linuxserver/sonarr
          tag: latest
        env:
          PUID: "1000"
          PGID: "1000"
          TZ: America/Los_Angeles
        envFrom:
          - secretRef:
              name: arr-api-key

service:
  main:
    controller: main
    annotations:
      gethomepage.dev/enabled: "true"
      gethomepage.dev/name: "Sonarr"
      gethomepage.dev/description: "TV shows"
      gethomepage.dev/group: "Media"
      gethomepage.dev/icon: "sonarr.png"
      gethomepage.dev/href: "https://sonarr.nik-homelab.dev"
    ports:
      http:
        port: 8989

route:
  main:
    enabled: true
    kind: HTTPRoute
    hostnames:
      - sonarr.nik-homelab.dev
    parentRefs:
      - name: homelab
        namespace: envoy-gateway-system

persistence:
  config:
    type: persistentVolumeClaim
    storageClass: local-path
    size: 2Gi
    accessMode: ReadWriteOnce
    globalMounts:
      - path: /config
  data:
    type: nfs
    server: 192.168.30.194
    path: /mnt/storage
    globalMounts:
      - path: /data
