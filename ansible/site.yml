---
- name: Apply common baseline to all nodes
  hosts: k3s_cluster
  tags: common          # without this `just provision-common` selects zero tasks
  roles:
    - common

- name: Deploy k3s cluster
  ansible.builtin.import_playbook: k3s.orchestration.site

- name: Configure NFS server on worker-01
  hosts: worker-01
  roles:
    - nfs_server

- name: Configure NFS client on worker-00 and worker-02
  hosts: worker-00,worker-02
  roles:
    - nfs_client

- name: Configure cert-manager ClusterIssuers on all server nodes
  hosts: k3s_cluster
  roles:
    - cert_manager_issuers
