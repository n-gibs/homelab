providers:
  kubernetesGateway:
    enabled: true
  kubernetesIngress:
    enabled: true

gatewayClass:
  enabled: true
  name: traefik

service:
  type: LoadBalancer

ingressRoute:
  dashboard:
    enabled: false  # expose via Gateway API instead
